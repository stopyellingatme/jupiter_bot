defmodule JupiterBot.Jupiter.Accounts do
  def get_perpetual_market_account(_market) do
    # Fetch market state account
  end

  def get_user_position_account(_user_pubkey, _market) do
    # Derive and fetch user's position account
  end

  def get_margin_account(_user_pubkey) do
    # Get user's margin account
  end
end
